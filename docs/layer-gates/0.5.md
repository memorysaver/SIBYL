# Layer 0.5 gate — UX-flow alignment (SIBYL persona + CLI-native originate skill)

> Evidence for `layer_gates[0.5]` in `product-context.yaml` (SIBYL). Two-phase gate:
> `scripted_passed` (Tier-1 framework tests green) → `passed` (all applicable tiers green **+** every
> acceptance criterion proven **+** prior-layer journeys replay green).

**Status:** `passed`   ·   **Date:** 2026-07-02   ·   **Run by:** main session (Claude) + live Codex agent via microsoft/shell-use

## Coverage summary

- **Criteria proven:** 3 / 3 (SIBYL-012)
- **Tiers (applicable only):** Tier-1 scripted **PASS** (100 tests) · Tier-2 journey **PASS** (01.2, live) · Tier-3 API **N/A**
- **Regression:** prior-layer (L0) journeys — machinery replayed green in the same 100-test suite; L0 gate remains `passed`

## Acceptance traceability

| Acceptance criterion (SIBYL-012) | Proving test | Verify | PASS/FAIL |
| --- | --- | --- | --- |
| System prompt leads with the SIBYL persona; agent self-identifies as SIBYL doing software engineering | `bundled-skill.test.ts` (persona index 0 of `getAppendSystemPrompt()`) + live | Asked "who are you?" → agent answered "SIBYL" + "software"; `SIBYL_PERSONA` is first in the assembled prompt | **PASS** |
| Guided-originate loaded as a **CLI-bundled** Pi skill (not a hardcoded brief), discoverable regardless of run cwd | `bundled-skill.test.ts` (`sibyl-originate` resolves from a throwaway temp dir while `aep-envision` does not; surfaced via `formatSkillsForPrompt`) + live | Journey 01.2 run from a cwd **outside** the SIBYL repo → chat showed `✓ read …/skills/sibyl-originate/SKILL.md`; `COCKPIT_GUIDE` string removed, replaced by a thin pointer | **PASS** |
| Pattern generalizes: bundling a later AEP skill needs no harness code change beyond registering it | `additionalSkillPaths: [SIBYL_BUNDLED_SKILLS_DIR, …]` in `createSibylResourceLoader` (path-list wiring, `import.meta.url`-relative) | Adding another skill dir/file to `apps/harness/skills` is discovered with no code change; `package.json` `files:[src,skills]` ships it | **PASS** |

## Scripted coverage (Tier-1)

- `apps/harness` vitest suite: **100 passed / 100** (18 files), `tsc --noEmit` clean. +3 over L0's 97: `bundled-skill.test.ts` (cwd-independent discovery, prompt-surfacing, persona-first).

## Journey evidence (Tier-2, live)

- **persona** (shell-use): agent self-identifies as SIBYL + software engineering when asked who it is.
- **journey 01.2 clean end-to-end** (shell-use, run from a cwd **outside** the SIBYL repo): agent read the bundled `sibyl-originate/SKILL.md` (cwd-independent discovery via `additionalSkillPaths`) → asked 3 focusing questions (pain point → tool details → CI stack), writing/committing nothing until focused → **proactively drafted** a focused README (`## Problem`/`## Vision`, 951 B, reflecting flaky-CI / trust-score / quarantine) → committed on approval (`77d6124 docs: add focused project README`) → Decisions tab rendered `- [originate] Committed README.md`. No raw git typed. Guided behavior unchanged, now sourced from the bundled skill.

## Notes / follow-ups

- The cockpit does not queue mid-turn input (concurrent prompts are rejected) — confirms mid-run steering (`steer`/`followUp`) as a future refinement, not a gate blocker.
- Bundling the `aep-*` skills (envision → map → …) into the CLI is the next step this pattern sets up (later layers).
