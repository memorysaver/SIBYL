# Layer 1 gate — Envision (first /aep-* drive on the AepFlow kernel)

> Evidence for `layer_gates[1]` in `product-context.yaml` (SIBYL). Two-phase gate:
> `scripted_passed` (Tier-1 green) → `passed` (all applicable tiers green + every acceptance
> criterion proven + prior-layer journeys replay green). Judged under `full_auto: true`
> (user mandate 2026-07-02); live dogfoods deferred to milestone gates per
> `dogfood.live_policy: milestone_gates_only`.

**Status:** `passed` · **Date:** 2026-07-02 · **Run by:** main session (Claude, orchestrator) + worktree subagents; zero-quota smokes via microsoft/shell-use

## Coverage summary

- **Criteria proven:** 16 / 16 (SIBYL-013: 4, SIBYL-014: 3, SIBYL-015: 3, SIBYL-017: 3, SIBYL-016: 3)
- **Tiers (applicable):** Tier-1 scripted **PASS** (178 tests / 20 files, tsc clean, ADR-001 scan green) · Tier-2 journey **PASS** (02.1 zero-quota live; 01 regression render half zero-quota live) · Tier-3 API **N/A**
- **Deferred by policy:** journey 02.2 (LIVE guided envision, ChatGPT quota) — authored as the build deliverable, EXECUTION rides the **L2 milestone gate**
- **Regression:** L0/L0.5 machinery green in the same suite; journey 01's zero-quota render half re-run live at this gate (below)

## Acceptance traceability

| Story | Criteria | Proving evidence | PASS |
| --- | --- | --- | --- |
| SIBYL-013 kernel | detectPhase purity; single-skill prompt narrowing; guard allow/deny; new-phase = registry-entry-only | `flow.test.ts` (35 tests: fake probes + real temp git repo; planted `user-decoy` skill excluded; dummy-phase AC4) | **4/4** |
| SIBYL-014 submit_envision | schema reject w/ actionable errors; valid → harness writes+commits `product/index.yaml` + hooks; raw write blocked, submit exempt | `submit.test.ts` (18 tests incl. real temp-repo commit; guard-deny + submit-succeed pair) | **3/3** |
| SIBYL-015 envision brief | bundled discovery w/ zero code change; body = README-context / ask-before-write / submit-only; envision session surfaces ONLY sibyl-envision | `bundled-skill.test.ts` (+4) incl. decoy-exclusion via the kernel's real narrowing path | **3/3** |
| SIBYL-017 brief injection | brief body verbatim in prompt; originate migrated off the pointer bet; frontmatter stripped / ordering / missing-brief boot error | `flow.test.ts` (+5) + `conversation.test.ts` (+2); ordering persona→role-line→body asserted by index | **3/3** |
| SIBYL-016 cockpit envision | detect-state boot into envision (prompt + UI); Architecture tab re-renders on submit completion; journey 02 authored + 02.1 passes | `conversation.test.ts` (+9) + `cockpit.test.ts` (+5); 02.1 run LIVE via shell-use (below) | **3/3** |

## Scripted coverage (Tier-1)

- `apps/harness` vitest: **178 / 178** (20 files), `bunx tsc --noEmit` clean. +78 over L0.5's 100
  across the four L1 merges (`be97c74`, `9d2741c`, `459b1e4`→157, `4aa668e`→164, `7bfb629`→178).

## Journey evidence (Tier-2, zero-quota, live TTY via shell-use)

- **Journey 02.1 (envision render smoke)** — run by the SIBYL-016 build agent from a temp repo
  (committed README, no product/index.yaml): header `[envision]`, 4 tabs, README in Goal,
  Architecture empty-state ("No product/index.yaml yet…"), `○ ready`; 8/8 expect-text
  assertions; git log unchanged after; **no prompt ever submitted**.
- **Journey 01 regression (originate render half)** — re-run at the gate by the orchestrator
  from an EMPTY repo: `[originate]` header, all 4 tabs, `ready` — 6/6 assertions; repo left
  with zero commits.
- **Beyond-registry fallback observed live (incidental):** launching the cockpit on the SIBYL
  repo itself (committed README + product/index.yaml) rendered `[envision]` with the fallback
  note instead of crashing — exactly the designed degradation until L2 registers post-envision
  phases.

## Notes / follow-ups

- Journey 02.2 (live guided envision → committed schema-valid `product/index.yaml`) executes at
  the **L2 milestone gate** together with the map/story-map dogfood.
- `yaml` is consumed as a hoisted transitive dep in the renderer — pin as a direct
  `apps/harness` dependency when the legibility module lands (L2).
- The Phase Pattern (docs/decisions/sibyl-phase-pattern.md) is now vertically proven for one
  phase; L2 begins the instantiation cadence (map = brief + submit_map + registry entry) plus
  the legibility engine.
