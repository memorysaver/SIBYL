{
  "name": "harness",
  "version": "0.0.0",
  "private": true,
  "description": "SIBYL TUI harness — boots the Pi AgentSession and binds the SIBYL engine-extension.",
  "type": "module",
  "main": "src/engine/session.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.80.2",
    "@earendil-works/pi-tui": "0.80.2"
  },
  "devDependencies": {
    "@types/node": "~22.19.0",
    "typescript": "~5.9.0",
    "vitest": "^3.2.6"
  },
  "engines": {
    "node": ">=22.19"
  }
}
